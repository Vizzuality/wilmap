/**
 * @file
 * Block WILMap behaviors.
 */

(function ($, Drupal) {

    'use strict';

    /**
     * WILMap Behaviors
     *
     * @type {Drupal~behavior}
     *
     * @prop {Drupal~behaviorAttach} attach
     *   Attaches the behavior for the block filtering.
     */
    Drupal.behaviors.wilmap = {
        attach: function (context, settings) {

            // ...
            // alert("Map loaded!");

        }
    };

}(jQuery, Drupal));
